# Notebooks

The notebooks are numbered in their intended execution order. Run them from this directory so relative paths resolve consistently:

- datasets: `../data/`
- model artifacts: `../models/`
- report outputs: `../reports/`

As the project matures, stable logic from these notebooks should move into `src/polymarket_irl/` and notebooks should become thin experiment or reporting layers.
