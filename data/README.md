# Data Directory

This directory stores local parquet datasets and intermediate feature tables used by the notebook workflow.

Large data artifacts are intentionally ignored by Git. Keep canonical datasets in a durable external store and document retrieval steps here when the data source is finalized.
