# Models Directory

This directory stores trained model checkpoints and exported estimators produced by the training notebooks.

Model binaries are intentionally ignored by Git. Use an artifact store or release process for checkpoints that need to be shared reproducibly.
